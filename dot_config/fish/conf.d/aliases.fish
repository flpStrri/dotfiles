# Navigation
alias ... 'cd ../..'
alias .... 'cd ../../..'

# Better defaults
alias ls 'ls -G'
alias ll 'ls -lah'
# alias cat 'bat --style=plain'
alias grep 'grep --color=auto'

# Git shortcuts
alias g git
alias gs git-st
alias gc 'git commit'
alias gp 'git push'
alias gl 'git pull'
